set nocompatible              " be iMproved, required

filetype plugin indent on    " required
" To ignore plugin indent changes, instead use:
"filetype plugin on

"Use 256 colours
set t_Co=256
"Show line numbers
set number
"Colour Scheme
colorscheme delek
"Turn on Vim's syntax highlighting
syntax on
"Number of spaces for  manual tabs
set tabstop=4
"Number of spaces to use for autoindent. (Used for >>, << and cindent)
set shiftwidth=4
"Auto indent... duhh
set autoindent
"Open vertical split in new window
set splitright
"Highlight search results
set hlsearch
"Always show status line on a window
set laststatus=2
"realtive file path in status line
"set statusline+=%f 
" Show full file path in statusline
set statusline+=%F
"Shortcuts for navigating Vim tabs - open new tab with tabe filename
map <C-t><up> :tabr<cr>
map <C-t><down> :tabl<cr>
map <C-t><left> :tabp<cr>
map <C-t><right> :tabn<cr>
"Paste current buffer on next line
nmap <C-p> :pu<cr>
"Highlight any text that exceeds a character length of 80
"highlight OverLength ctermbg=red ctermfg=white guibg=#592929
"match OverLength /\%85v.\+/
highlight ColorColumn ctermbg=DarkGrey
set colorcolumn=80
"let &colorcolumn=join(range(81,999),",") "Highlight screen from column to end
"Automatically put asterisks for block comments - Start comment with /**
set formatoptions+=r " Default value is tcq 
"Make autocomplete case insensitive
set ignorecase
"Make Vim correct case when there is matches of different case
set infercase

"Autocomplete for source code symbols. Type <C-X><C-O> to show menu 
"of matches and navigate with <C-N> and <C-P> 
filetype plugin on
set omnifunc=syntaxccomplete#Complete
" Copy rest of line (NORMAL)- <y><$>

" Map Inner word yank and deletion to entire word
"onoremap w iw
